requests
colorama
paramiko
python-whois
python-nmap
beautifulsoup4

# ── New modules (v2.2) ────────────────────────────────────────────────────────
# Service brute-force: MySQL and PostgreSQL support
pymysql
psycopg2-binary

# Note: No external crypto library needed
# crypto_utils.py uses Python stdlib (hashlib, hmac, base64) only
# This keeps CSCAN lightweight and Termux-compatible

# Advanced Browser Engine for Cloudflare WAF bypass, JS rendering, and browser-based checks
# Requires a Playwright-compatible desktop/server environment (not Termux)
camoufox[geoip]
# After install run: python -m camoufox fetch
